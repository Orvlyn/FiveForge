# Core dependencies
PySide6>=6.6.0
Pillow>=10.0.0
pythonnet>=3.0.5,<4
lxml>=4.9.0

# .exe packaging
pyinstaller>=6.0.0

# Optional: DDS conversion (needed for replacing textures with PNG)
# Requires ImageMagick installed: https://imagemagick.org
# Wand>=0.6.0

# Notes:
# - Use Python 3.12 or 3.13 for best pythonnet compatibility on Windows.
# - Python 3.14 may fail to install pythonnet wheels.
